<UserControl x:Class="FACTOVA_QueryHelper.Controls.QueryManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#FFF8F9FA">
    
    <Grid Margin="20">
        <TabControl x:Name="QueryTypeTabControl"
                    Background="White"
                    BorderBrush="#FFE0E0E0"
                    BorderThickness="1"
                    SelectionChanged="QueryTypeTabControl_SelectionChanged">
            <TabControl.Resources>
                <!-- 탭 헤더 스타일 -->
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border"
                                        Background="Transparent"
                                        BorderBrush="#FFE0E0E0"
                                        BorderThickness="1,1,1,0"
                                        CornerRadius="8,8,0,0"
                                        Padding="20,12"
                                        Margin="2,0,0,0">
                                    <ContentPresenter x:Name="ContentSite"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="White"/>
                                        <Setter TargetName="Border" Property="BorderBrush" Value="#FF0078D7"/>
                                        <Setter TargetName="Border" Property="BorderThickness" Value="2,2,2,0"/>
                                        <Setter Property="Panel.ZIndex" Value="100"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="#FFF8F9FA"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#FFF0F8FF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Foreground" Value="#FF495057"/>
                </Style>
            </TabControl.Resources>

            <!-- 쿼리 실행 탭 -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="▶" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="쿼리 실행"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid x:Name="QueryExecutionGrid" Margin="20"/>
            </TabItem>

            <!-- 정보 조회 탭 -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="정보 조회"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid x:Name="InfoQueryGrid" Margin="20"/>
            </TabItem>

            <!-- 비즈 조회 탭 -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔍" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="비즈 조회"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid x:Name="BizQueryGrid" Margin="20"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
