<Window x:Class="FACTOVA_QueryHelper.Controls.TabRenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="탭 이름 변경" 
        Height="180" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Grid.Row="0" 
                   Text="탭 이름을 입력하세요:" 
                   FontWeight="SemiBold" 
                   FontSize="13"
                   Margin="0,0,0,10"/>

        <!-- 입력 필드 -->
        <TextBox x:Name="TabNameTextBox" 
                 Grid.Row="1"
                 FontSize="13"
                 Padding="5"
                 MaxLength="50"
                 KeyDown="TabNameTextBox_KeyDown"/>

        <!-- 버튼 -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="확인" 
                    Width="80" 
                    Height="30"
                    Margin="0,0,10,0"
                    Background="#FF2196F3"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="OkButton_Click"
                    IsDefault="True"/>
            <Button Content="취소" 
                    Width="80" 
                    Height="30"
                    Background="#FF6C757D"
                    Foreground="White"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
