<Window x:Class="FACTOVA_QueryHelper.Controls.UpdateNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Notification" 
        Height="400" 
        Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Icon and Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <Border Width="48" Height="48" Background="#0078D4" CornerRadius="24" Margin="0,0,15,0">
                <TextBlock Text="&#x1F514;" FontSize="28" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="New Version Available!" FontSize="20" FontWeight="Bold" Foreground="#333"/>
                <TextBlock x:Name="VersionInfoTextBlock" Text="Current: v1.0.0 -> Latest: v1.1.0" 
                          FontSize="12" Foreground="#666" Margin="0,5,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Update Content Title -->
        <TextBlock Grid.Row="1" Text="Release Notes:" FontSize="14" FontWeight="Bold" 
                  Margin="0,0,0,10" Foreground="#333"/>

        <!-- Release Notes -->
        <Border Grid.Row="2" Background="White" BorderBrush="#DDD" BorderThickness="1" 
                CornerRadius="5" Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ReleaseNotesTextBlock" 
                          TextWrapping="Wrap" 
                          FontSize="12" 
                          Foreground="#555"
                          LineHeight="20"/>
            </ScrollViewer>
        </Border>

        <!-- Auto Check Option -->
        <CheckBox x:Name="AutoCheckCheckBox" 
                 Grid.Row="3" 
                 Content="Check for updates on startup" 
                 IsChecked="True"
                 Margin="0,15,0,0"
                 FontSize="12"/>

        <!-- Button Area -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="DownloadButton" 
                   Content="Download" 
                   Background="#0078D4" 
                   Foreground="White"
                   Click="DownloadButton_Click"
                   MinWidth="100"/>
            <Button x:Name="LaterButton" 
                   Content="Later" 
                   Background="#E0E0E0"
                   Click="LaterButton_Click"
                   MinWidth="100"/>
        </StackPanel>
    </Grid>
</Window>
